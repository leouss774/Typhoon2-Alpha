# --- Backend / orchestrateur ---
fastapi
uvicorn[standard]
pydantic>=2
pydantic-settings

# --- Orchestration multi-agents (prochaine etape) ---
langgraph
langchain
langchain-anthropic
langchain-community

# --- Appels API / HTTP ---
httpx
requests

# --- Lookup local DVF ---
pandas

# --- Copernicus Climate Data Store (projections climatiques) ---
cdsapi
xarray
netCDF4

# --- RAG (prochaine etape) ---
chromadb

# --- Agent recommandations (RAG travaux, cf. app/recommandations/) ---
# Le code (app/recommandations/mistral_client.py) utilise l'API 2.x
# (Mistral(api_key=..., timeout_ms=...), client.chat.complete,
# client.embeddings.create) : il faut donc mistralai>=2.0.0. L'import se
# fait via `from mistralai.client import Mistral` (l'init de paquet
# top-level n'est pas present dans toutes les builds 2.x).
mistralai>=2.0.0,<3.0.0
numpy>=1.26.0

# --- Divers ---
python-dotenv
sqlalchemy

# --- Tests ---
pytest
pytest-asyncio
mistralai
