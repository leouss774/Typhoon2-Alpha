# --- Backend / orchestrateur ---
fastapi
uvicorn[standard]
pydantic>=2
pydantic-settings

# --- Orchestration multi-agents (prochaine etape) ---
langgraph
langchain
langchain-anthropic
langchain-community

# --- Appels API / HTTP ---
httpx

# --- Lookup local DVF ---
pandas

# --- Copernicus Climate Data Store (projections climatiques) ---
cdsapi
xarray
netCDF4

# --- RAG (prochaine etape) ---
chromadb

# --- Agent recommandations (RAG travaux, cf. app/recommandations/) ---
mistralai>=1.5.0
numpy>=1.26.0

# --- Zone-Insurer : cache de duplication (Postgres en prod / SQLite en dev) ---
psycopg[binary]>=3.1

# --- Divers ---
python-dotenv
sqlalchemy
